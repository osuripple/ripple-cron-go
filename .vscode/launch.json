{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run cron",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cron.go",
            "args": ["-v"],
        }
    ]
}